# ============================================
# VELA 백엔드 서버 환경변수 설정 파일
# 이 파일을 .env로 복사한 뒤 값을 채워주세요
# cp .env.example .env
# ============================================

# 서버 포트 (프론트엔드 Vite가 5173을 사용하므로 3001 권장)
PORT=3001

# Mock 모드 (true로 설정하면 RunPod 없이 테스트 가능)
MOCK_MODE=true

# RunPod Serverless API 설정
RUNPOD_API_KEY=your_key_here
RUNPOD_ENDPOINT_ID=your_endpoint_here

# Whisper 음성 인식 설정
# 모델 크기: tiny, base, small, medium, large
WHISPER_MODEL=base
# 기본 언어: ko(한국어), en(영어), ja(일본어)
WHISPER_LANGUAGE=ko

# 파일 자동 정리 (시간 단위, 기본 24시간)
FILE_CLEANUP_HOURS=24
