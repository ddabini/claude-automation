# ============================================
# VELA GPU 워커 의존성 패키지 목록
# RunPod 서버리스 GPU 환경에서 실행됩니다
# ============================================

# --- RunPod 서버리스 SDK ---
# GPU 워커와 RunPod 플랫폼을 연결해주는 라이브러리
runpod>=1.7.0

# --- PyTorch (딥러닝 프레임워크) ---
# CUDA 12.1 환경에 맞는 PyTorch (Dockerfile에서 이미 설치되므로 주석 처리 가능)
# torch>=2.2.0

# --- Diffusers (영상 생성 파이프라인) ---
# HuggingFace의 AI 모델 실행 프레임워크
# Wan 2.1 지원은 diffusers 0.32.0+ 부터
diffusers>=0.32.0

# --- Transformers (AI 텍스트/이미지 인코더) ---
# 프롬프트 텍스트를 모델이 이해하는 형태로 변환
transformers>=4.46.0

# --- Accelerate (모델 로딩 최적화) ---
# 대형 모델을 GPU에 효율적으로 로드
accelerate>=1.2.0

# --- Safetensors (모델 파일 포맷) ---
# HuggingFace 모델 가중치를 안전하게 로드
safetensors>=0.4.0

# --- 이미지 처리 ---
# 입력 이미지 전처리에 사용
Pillow>=10.0.0
numpy>=1.24.0

# --- SentencePiece (T5 텍스트 인코더용) ---
# Wan 2.1이 텍스트를 처리할 때 내부적으로 사용
sentencepiece>=0.2.0

# --- ftfy (텍스트 정리) ---
# 입력 프롬프트의 특수문자 등을 깔끔하게 처리
ftfy>=6.0

# --- imageio + imageio-ffmpeg (영상 파일 저장) ---
# 생성된 프레임을 MP4 파일로 변환
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
